\* SeqCst stores only: HOLDS on x86 (XCHG drains the buffer) but NOT in the Rust
\* memory model -- see the note at the bottom of BfTreeCprHandshake.tla.
CONSTANTS Fix = "SeqCstStores"
SPECIFICATION Spec
INVARIANT PhaseCompletionIsSound
